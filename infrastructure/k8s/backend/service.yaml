apiVersion: v1
kind: Service
metadata:
  name: instantdeploy-backend
  namespace: instantdeploy
spec:
  type: NodePort
  selector:
    app: instantdeploy-backend
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: http
      nodePort: 30080
